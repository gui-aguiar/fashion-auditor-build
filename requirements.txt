# FashionAuditor standalone build — runtime deps
# (Tkinter ships with Python; Pillow decodes/scales the product images)
pillow>=10.0

# Dev/build only:
#   pip install pyinstaller   # to produce the executable (build on the TARGET OS)
# Tests:  python -m unittest discover tests -v   (stdlib only, no extra deps)
